# Code Quality

- React hooks manage state; Redux is intentionally not used.
- Components are split by role and responsibility.
- Reusable UI primitives handle metrics, sections, and role tabs.
- PropTypes document runtime component contracts.
- Constants, data, services, hooks, utils, and docs are separated.
- Local AI routing avoids duplicate response logic and unnecessary remote calls.
