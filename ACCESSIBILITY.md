# Accessibility

- Uses semantic sections, headings, tables, buttons, labels, and form controls.
- Role switcher uses `tablist` and `tab` semantics.
- Assistant messages use `aria-live` for response updates.
- Inputs and selects include accessible labels.
- Focus states are visible across interactive controls.
- Layouts are responsive from mobile to wide desktop.
