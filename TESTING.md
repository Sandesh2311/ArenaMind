# Testing

Testing uses Vitest, React Testing Library, and V8 coverage.

```bash
npm test
```

Current coverage focuses on:

- Prompt validation.
- Local AI fallback routing.
- Assistant interaction.
- Role dashboard switching.

Coverage thresholds are configured in `vite.config.js`.
